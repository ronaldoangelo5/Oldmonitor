
    <?php
        for($i=0;$i<=$d99-1;$i++){
            $c1[] = pow($d6[$i], 2);
            $c2[] = pow($d7[$i], 2);
            $c3[] = pow($d8[$i], 2);
            $c4[] = pow($d9[$i], 2);
        }

        /*
        for($i=0;$i<=$d99-1;$i++){
            $v1[] = $d2[$i];
            $v2[] = sprintf("%0.4f", $d6[$i] / sqrt(array_sum($c1)));
            $v3[] = sprintf("%0.4f", $d7[$i] / sqrt(array_sum($c2)));
            $v4[] = sprintf("%0.4f", $d8[$i] / sqrt(array_sum($c3)));
            $v4[] = sprintf("%0.4f", $d9[$i] / sqrt(array_sum($c4)));
        }
        */

        ?>